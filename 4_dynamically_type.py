x = 25
print(type(x))

x = 15.6
print(type(x))

x = 'Garg Studio'
print(type(x))

# as a example Java is a Statically Type Language. We cannot change a defined variable value in runtime. Also we should provide a value when we define the variable.
# But in Python we can change the defined variable value in runtime. So Python is a Dynamicall Type language.